# Salesforce Administrator 学習メモ

このメモは、練習問題を解くために必要な判断軸を読み物として整理したものです。問題文を覚えるのではなく、「どの要件ならどの機能を選ぶか」を判断できる状態を目指します。

## まず押さえる全体像

- Salesforce の設定問題は、ほとんどが「データ構造」「画面」「権限」「自動化」「分析」「サポート業務」のどれかに分類できる。
- 問題文に「誰が何を見られるか」が出たら、まずオブジェクト権限、項目レベルセキュリティ、OWD、役割階層、共有ルールを切り分ける。
- 問題文に「保存時に止めたい」が出たら入力規則を考える。
- 問題文に「条件に応じて処理したい」が出たら Flow Builder を第一候補にする。
- 問題文に「承認してから進めたい」が出たら承認プロセスを考える。
- 問題文に「見込み客、未評価、商談化前」が出たらリードを考える。
- 問題文に「問い合わせ、苦情、トラブル、解決時間」が出たらケースを考える。
- 問題文に「売上、金額、フェーズ、完了予定日、成約、失注」が出たら商談を考える。

## データモデルと関係

- 参照関係は、別オブジェクトのレコードをゆるく関連付ける関係。親を削除しても子を必ず削除する要件でなければ、まず参照関係を考える。
- 主従関係は、親が子の所有権や共有を制御する強い関係。親を削除すると子も削除される。積み上げ集計項目を標準機能で使いたい場合も主従関係が重要。
- 多対多は、連結オブジェクトを作り、両側に主従関係を持たせるのが基本。
- 階層関係はユーザー間の上長、承認者、メンターなどを表す特殊な関係。
- 外部参照関係は、親が外部オブジェクトになる関係。
- 間接参照関係は、子が外部オブジェクトで、親が標準またはカスタムオブジェクトになる関係。外部 ID を使って関連付ける。
- ルックアップ検索条件は、参照項目で選択できる候補レコードを条件で絞る機能。表示項目数を決める機能ではない。
- クロスオブジェクト数式は、関連先オブジェクトの項目値を数式で参照したいときに使う。
- 連動選択リストは、親の選択値に応じて子の選択肢を絞る機能。

## 標準オブジェクトの判断軸

- リードは、まだ営業対象として適格か判断していない見込み客。展示会、資料請求、Webフォーム流入などが典型。
- 取引先は、企業や組織を表す。
- 取引先責任者は、取引先に所属する個人を表す。
- 商談は、売上や案件化した取引を管理する。金額、フェーズ、完了予定日、成約、失注がキーワード。
- ケースは、問い合わせ、苦情、障害、トラブルなど顧客サポートを管理する。
- キャンペーンは、展示会、セミナー、広告、メール配信などマーケティング施策を管理する。
- 活動は、電話、会議、ToDo、メールなど「誰に何をしたか」の記録。
- 商品と価格表は、「何を、いくらで売るか」を定義する。
- リード取引開始では、取引先、取引先責任者、必要に応じて商談を作成できる。

## プロセスとレコードタイプ

- レコードタイプは、同じオブジェクトの中でページレイアウトや選択リスト値を分けたいときに使う。
- 商談フェーズを分けるときは販売プロセスを使う。
- ケースの状況を分けるときはサポートプロセスを使う。
- リード状況を分けるときはリードプロセスを使う。
- パスは、フェーズごとに重要な項目と成功へのガイダンスを表示する機能。商談フェーズに沿った営業支援でよく問われる。
- ケースのクローズ時に入力させる項目は、ケースクローズページレイアウトを考える。

## 権限と共有

- プロファイルはユーザーに必ず1つ割り当てる基本権限。オブジェクト権限、アプリケーション、ログイン時間帯などの基礎になる。
- 権限セットは、既存プロファイルを変えずに追加権限を付与するために使う。1ユーザーに複数割り当てられる。
- 権限セットグループは、複数の権限セットを職務単位でまとめるために使う。
- ミュート権限セットは、権限セットグループ内の一部権限を無効化するために使う。
- オブジェクト権限がないユーザーに共有ルールでレコードを共有しても、そのユーザーは参照できない。共有はオブジェクト権限を超えない。
- OWD は、オブジェクトごとのレコードアクセスの基準値。まずどこまで閉じるかを決める。
- 役割階層は、上位ロールのユーザーに下位ユーザーのレコードアクセスを広げるために使う。
- 共有ルールは、OWD で閉じたデータを条件や所有者に基づいて追加共有する機能。
- 手動共有は、個別レコードを例外的に特定ユーザーへ共有するときに使う。
- 項目レベルセキュリティは、項目を表示できるか、編集できるかを制御する。ページレイアウトより強い。
- ページレイアウトに項目を置いても、項目レベルセキュリティで参照不可なら表示されない。
- ページレイアウトで必須でも、項目レベルセキュリティで読み取り専用なら編集できない。

## ユーザー管理とセキュリティ

- Salesforce ユーザーは完全削除せず、退職時などは無効化する。
- 無効化できない場合は、自動処理、メールアラート、階層カスタム項目、テリトリーなどで参照されていないかを疑う。
- 設定監査ログは、管理者が行った設定変更を確認する。
- ログイン履歴は、ログイン成功、失敗、ログイン方法などを確認する。
- セキュリティ状態チェックは、組織のセキュリティ設定を評価する。
- パスワードポリシーは、組織レベルのデフォルトとプロファイル単位の設定を理解する。
- 内部ユーザーの MFA は、Salesforce Authenticator、サードパーティ認証アプリ、物理セキュリティキーなどを使う。SMS やメールだけでは内部ユーザーの MFA 手段にならない。
- データローダーは API ログインとして扱われる。
- 個人情報保護関連では、リード、取引先責任者、個人取引先と個人（Individual）レコードの関連を押さえる。

## 画面とアプリ

- Lightning アプリケーションビルダーは、Lightning ページにコンポーネントを配置したり、条件に応じて表示を制御したりする。
- 動的フォームは、ユーザーやレコード値などの条件に応じて、項目やセクションを表示制御する。
- コンパクトレイアウトは、強調表示パネルなどに表示される主要項目を制御する。
- 関連リストは、関連レコードの一覧をレコードページに表示する。
- 分割ビューは、リストと詳細を横並びで表示する。
- モバイルナビゲーションでは、スマホで使うナビゲーション項目や順序を意識する。
- ブランド設定はテーマやロゴなど見た目の統一に関係する。

## 自動化

- 新規自動化は Flow Builder を第一候補にする。
- Workflow Rules と Process Builder は旧機能。既存は動作し得るが、Flow への移行が推奨される。
- 入力規則は、保存時に条件を満たしたらエラーにして保存を止める。
- 承認プロセスは、申請、承認者、承認後や却下後のアクションを定義する。
- 承認プロセスでは、メールアラート、項目自動更新、ToDo などを使える。
- レコードトリガーフローは、レコード作成や更新をきっかけに自動処理する。
- 画面フローは、ユーザー入力を受け取る対話型処理に使う。
- スケジュールトリガーフローは、毎日や毎月など定期実行に使う。
- `$Record` はトリガーされたレコードの現在値、`$Record__Prior` は更新前の値。変更前後の比較で使う。
- フローで別オブジェクトのレコードを条件検索するなら「レコードを取得」を使う。

## ケース管理とサービス

- Web-to-ケースは、Webフォームからケースを作成する。
- メール-to-ケースは、サポートメール宛の受信メールからケースを作成し、返信を既存ケースに紐付ける。
- ケース割り当てルールは、新規ケースの所有者をユーザーやキューに自動設定する。
- リード割り当てルールは、新規リードの所有者をユーザーやキューに自動設定する。
- 自動レスポンスルールは、ケースやリードを受け付けたときに自動返信する。
- エスカレーションルールは、一定時間未解決のケースをエスカレートする。
- オムニチャネルは、スキル、作業量、在席状況などに基づき、ケースやチャットなどの作業項目をルーティングする。
- Salesforce Knowledge は、FAQ や社内手順などの記事を管理する。
- 顧客向けセルフサービスでは、Experience Cloud サイトと Salesforce Knowledge を組み合わせる発想が重要。
- ケース画面で関連記事を検索、閲覧、添付するなら Knowledge コンポーネントを考える。

## レポートとダッシュボード

- レポートタイプは、レポートで使えるオブジェクトと関連を定義する。
- 表形式、サマリー、マトリックス、結合はレポート形式。
- 表形式は単純な一覧。
- サマリーはグループ化や小計に向く。
- マトリックスは行と列の両方でグループ化する。
- 結合レポートは複数ブロックを1つのレポート内で比較する。
- バケット列は、レポート内だけで値を分類する。元データに項目を追加しない。
- 条件付き強調表示は、集計値などに応じてセルを色分けする。
- レポートフォルダーの共有で、レポートの閲覧対象を制御する。
- 静的ダッシュボードは、実行ユーザーのアクセス権でデータを表示する。
- 動的ダッシュボードは、閲覧者自身のアクセス権に応じたデータを表示する。
- ダッシュボードはキャッシュされた結果を表示することがある。値が古い場合は更新を考える。
- ファネルチャートは段階ごとの推移や営業パイプラインに向く。
- ゲージは目標達成率の表示に向く。

## データ管理と ID

- Salesforce ID はレコードを一意に識別する。15桁と18桁がある。
- 18桁 ID は大文字小文字を区別しない環境でも扱いやすい。
- 外部 ID は、外部システムの顧客番号などを Salesforce と照合するために使う。
- Upsert は、外部 ID などで一致すれば更新し、一致しなければ追加する。
- レコードタイプ ID は、レコードタイプを識別する。
- 組織 ID は、Salesforce 組織そのものを識別する。サポート問い合わせなどで使う。
- データローダーは大量データのインポート、更新、削除などに使う。
- データのエクスポートは、レコードデータを zip 形式などで出力する。入力規則などのメタデータ取得用ではない。
- 削除済みレコードはリサイクルビンに最大15日保持される。ただし容量などにより早く削除される可能性がある。
- 標準の項目履歴管理は、通常1オブジェクトあたり最大20項目。UI で最大18か月、API 経由で最大24か月が基準。
- Field Audit Trail アドオンでは、履歴管理を拡張できる。

## AppExchange とコラボレーション

- AppExchange のネイティブアプリは Salesforce プラットフォーム上で動く。
- クライアントアプリは外部システム連携を主目的にする。
- コンポジットアプリは Salesforce 上の機能と外部システムの機能を組み合わせる。
- Chatter は投稿、コメント、グループで社内コラボレーションを行う。
- Chatter 公開グループは広く参加しやすい共有に使う。
- Chatter 非公開グループは招待メンバーだけで共有したい場合に使う。

## 試験直前の判断チェック

- 「見込み客、未評価」ならリード。
- 「会社、法人、組織」なら取引先。
- 「担当者、個人、メールアドレス」なら取引先責任者。
- 「金額、フェーズ、成約、失注」なら商談。
- 「問い合わせ、苦情、トラブル」ならケース。
- 「展示会、セミナー、広告、ROI」ならキャンペーン。
- 「何をしたか、電話、会議、ToDo」なら活動。
- 「参照候補を絞る」ならルックアップ検索条件。
- 「保存を止める」なら入力規則。
- 「承認が必要」なら承認プロセス。
- 「新規ケースやリードを誰かに割り振る」なら割り当てルール。
- 「未解決ケースを時間で上げる」ならエスカレーションルール。
- 「自動返信」なら自動レスポンスルール。
- 「新規自動化」なら Flow Builder。
- 「どのレコードが見えるか」なら OWD、役割階層、共有ルール。
- 「どの項目が見えるか」なら項目レベルセキュリティ。
- 「ページ上の項目やコンポーネント表示」なら Lightning アプリケーションビルダー、動的フォーム、コンパクトレイアウト。
- 「レポートで使えるオブジェクト」ならレポートタイプ。
- 「レポートの形」なら表形式、サマリー、マトリックス、結合。
